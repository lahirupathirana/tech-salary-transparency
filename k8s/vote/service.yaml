apiVersion: v1
kind: Service
metadata:
  name: vote
  namespace: payfloor
  labels:
    app.kubernetes.io/name: vote
    app.kubernetes.io/part-of: payfloor
spec:
  type: ClusterIP
  selector:
    app: vote
  ports:
    - name: http
      port: 4003
      targetPort: 4003
